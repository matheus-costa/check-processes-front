export interface Process {
    code: string,
    type: string,
    interested: string,
    registerDate: Date,
    urlQuery: string,
    lastQuery: Date,
    status: string,
    lastDocument: number;
}